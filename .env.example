# Private case intake is disabled until independent authentication and storage are provisioned.
# Never copy credentials or production data from 360 Degrees.

# Eden Authority Service rollout mode: off | shadow | enforce
EDEN_AUTHORITY_MODE=off
# Server-only. Do not expose either value through NEXT_PUBLIC_* variables.
EDEN_AUTHORITY_URL=
EDEN_AUTHORITY_API_KEY=
